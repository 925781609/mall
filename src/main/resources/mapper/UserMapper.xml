<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.liuil.web.mappe.UserDao">
  <resultMap id="BaseResultMap" type="com.liuil.web.domain.User">
    <result column="id" property="id" />
    <result column="username" property="username" />
    <result column="password" property="password" />
    <result column="email" property="email" />
    <result column="phone" property="phone" />
    <result column="role" property="role" />
    <result column="is_confirmed" property="confirmed" />
    <result column="is_active" property="activate" />
    <result column="gmt_create" property="createTime" />
    <result column="gmt_modified" property="modifiedTime" />
  </resultMap>

  <sql id="Base_Column_List">
    id, username, password, email, phone, role, is_confirmed, is_activate
  </sql>

  <select id="selectPasswordByUsername"  parameterType="java.lang.String" resultMap="BaseResultMap">
    SELECT
    password
    FROM user
    WHERE username = #{username}
  </select>

  <select id="findByName"  parameterType="java.lang.String" resultMap="BaseResultMap">
    SELECT
    <include refid="Base_Column_List" />
    FROM user
    WHERE username = #{username}
  </select>

  <select id="findByEmail"  parameterType="java.lang.String" resultMap="BaseResultMap">
    SELECT
    <include refid="Base_Column_List" />
    FROM user
    WHERE email = #{email}
  </select>

  <insert id="save" parameterType="com.liuil.web.domain.User" >
    INSERT INTO
    user
    (username, password, email, phone, gmt_create, gmt_modified)
    VALUES
    (#{username}, #{password}, #{email}, #{phone}, now(), now())
  </insert>
</mapper>
